# json2csv (via json2viz repository)

A command-line utility built with Python and Pandas to quickly flatten nested JSON or JSON Lines (JSONL) files into a single, clean CSV file.

## 🚀 Installation

Ensure you have Python 3 installed. You can install this utility directly from this GitHub repository using pip:

```bash
pip install git+[https://github.com/Nagoyashi/json2viz.git](https://github.com/Nagoyashi/json2viz.git)